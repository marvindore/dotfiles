#!/usr/bin/env bash

# Install Eclipse for Enterpise Java Developers + Add C++ support
# SDKMan
# curl -s "https://get.sdkman.io" | bash
# source "$HOME/.sdkman/bin/sdkman-init.sh"
# ln -s ~/.sdkman ~/sdkman

brew update && brew install git bat zsh ripgrep yarn pnpm lf tmux pyenv nvm zoxide docker docker-compose rust magic-wormhole rclone neovim go lazygit curl asdf
# jabba
