# Lean prompt - no blocking environment checks
# Environment info available via `envcheck` function (on-demand)

function set_rprompt() {
  RPROMPT="$(date +'%H:%M')"
}

precmd_functions+=(set_rprompt)
